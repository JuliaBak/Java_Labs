Тема Кафе.
У нас имеется меню (menu), в котором позиции-блюда (meal). При этом meal может быть либо напитком (drink),
либо непосредственно блюдом (dish).

У meal есть атрибуты: название (name) и цена (price).

У dish есть атрибут type (значение которого может быть одним из представленного списка:
 type (appetizer|main|dessert)), причем этот атрибут явялется необязтельным.

Блюдо (dish) состоит из продуктов (product). Причем у подуктов есть атрибуты: name и себестоимость (cost_price).

У напитка (drink) есть атрибут type (значение которого может быть одним из представленного списка:
(coffee|tea|water|alcohol|fizzy_drink), причем этот атрибут явялется необязтельным.

Соответственно, при запуске MainTest в консоль выводится найденный элемент, его дочерние узлы и их атрибуты. 
